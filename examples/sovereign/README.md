# Sovereign doctrine examples

Offline demos for memory, safety, kill-switch awareness, and the filesystem sandbox.
No live LLM required.

## Run

```shell
python examples/sovereign/doctrine_demo.py
```

Expected plain-language output: config flags, a memory context block, a safety block message, and a write/read under `workspace/`.
